# Scripts that use this environment should contain this preamble:
#
# #!/bin/bash
# BIN_DIR="$(cd $(dirname ${BASH_SOURCE[0]}) && pwd)"
# source $BIN_DIR/.envrc
PROJECT=textteach

PROJ_DIR="$(dirname $BIN_DIR)"
ENV_DIR="$PROJ_DIR/ttenv"
