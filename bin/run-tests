#!/bin/bash
BIN_DIR="$(cd $(dirname ${BASH_SOURCE[0]}) && pwd)"
source $BIN_DIR/.envrc

cd $PROJ_DIR/app
python -m unittest discover -v --pattern='*_test.py'

TT_FILES="controller.py controller_test.py engine.py engine_test.py textteach.py"
pep8 --max-line-length=100 $TT_FILES
