#!/bin/bash
BIN_DIR="$(cd $(dirname ${BASH_SOURCE[0]}) && pwd)"
source $BIN_DIR/.envrc

cd $PROJ_DIR
pwd
python -m unittest discover --pattern='*_test.py'
pep8 --max-line-length=100 engine.py engine_test.py controller.py controller_test.py
